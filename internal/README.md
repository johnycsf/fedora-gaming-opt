# Internal implementation

`manage.sh` is the supported entry point for installation, maintenance, and rollback.
Files in this directory are implementation details called by that command; they remain
versioned for review, but are not intended to be run directly.
