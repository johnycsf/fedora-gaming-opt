# Keep global environment changes intentionally minimal.
# Do NOT export ENABLE_GAMESCOPE_WSI, DXVK_*, RADV_*, or Proton flags here.
# Those belong in per-game launch options. See docs/LESSONS.md.
export MESA_SHADER_CACHE_MAX_SIZE=512MB
